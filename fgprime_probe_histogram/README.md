# "Fine-grained" Prime+Probe Histogram
Prints multiple Prime+Probe histograms, depending on how many ways have been evicted. 

# Run Code
Run `make` then `./hist`

# Works on 
C906 (because of FIFO cache replacement strategy)